---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: bff
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
    - http:
        paths:
        - path: /time
          backend:
            serviceName: bff
            servicePort: 80